---
interface Props {
	categories?: string[];
}

const { categories } = Astro.props;
---

{
	!!categories && (
		<ul class="category-list cluster" role="list">
			{categories?.map((category) => (
				<li>{category}</li>
			))}
		</ul>
	)
}
