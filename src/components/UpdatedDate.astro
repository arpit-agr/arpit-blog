---
interface Props {
	date?: Date;
}

const { date } = Astro.props;
---

<p class="text-box-trim">
	Updated on
	<time datetime={date?.toISOString()}>
		{
			date?.toLocaleString("en-IN", {
				weekday: "long",
				year: "numeric",
				month: "short",
				day: "numeric",
			})
		}
	</time>
</p>
